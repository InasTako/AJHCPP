#include<iostream>
using namespace std;

int main()
{
    // 회문 판별 알고리즘
    /*
    string word;
    bool check = true;

    cin >> word;
    
    for (int i = 0; i < word.length() / 2; i++)
    {
        if (word[i] != word[word.length() - 1 - i])
        {
            check = false;
        }
    }
    if (check == true)
    {
        cout << "회문입니다." << endl;
    }
    else
    {
        cout << "회문이 아닙니다." << endl;
    }
    */

    // 접미사
    // 리터럴 상수 값은 double 자료형으로 치환됩니다. 
    auto value = 4.5f;

    return 0;
}